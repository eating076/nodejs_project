<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>睡眠測試記錄</title>
    <link rel="stylesheet" 
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background: linear-gradient(to right, rgba(0, 123, 255, 0.1), rgba(0, 123, 255, 0.2)), 
                        url('https://via.placeholder.com/1500x1000/87CEFA/ffffff?text=Background+Image') no-repeat center center fixed;
            background-size: cover;
            font-family: 'DFKai-SB', '標楷體', DFKai-SB, serif;
            color: #333;
        }

        .container {
            margin-top: 50px;
            max-width: 90%;
            margin-left: auto;
            margin-right: auto;
        }

        /* 外層的白框，增加陰影和圓角 */
        .white-box {
            background-color: #fff;
            border-radius: 16px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-bottom: 30px;
            overflow: hidden; /* 防止內容溢出白框 */
        }

        .table {
            background-color: #ffffff;
            border-radius: 10px;
            width: 100%; /* 讓表格充滿整個容器 */
            word-break: break-word; /* 防止文字超出格子 */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .table th {
            background-color: #343a40;
            color: white;
        }

        .table td {
            vertical-align: middle;
            padding: 12px; /* 給表格增加內邊距，避免內容擠在一起 */
        }

        .highlight {
            font-weight: bold;
            color: #d9534f; /* 高分數標註紅色 */
        }

        h2 {
            font-size: 30px;
            color: #4e73df;
            margin-bottom: 20px;
            font-weight: 700;
        }

        p {
            font-size: 18px;
            color: #333;
        }

        /* 新增的 hover 效果 */
        .table tbody tr:hover {
            background-color: #f1f1f1;
        }

        /* 小螢幕樣式 */
        @media (max-width: 768px) {
            .container {
                padding-left: 15px;
                padding-right: 15px;
            }

            h2 {
                font-size: 24px;
            }

            .table th, .table td {
                font-size: 14px;
            }
        }

        /* 左右滾動條的樣式 */
        .table-responsive {
            overflow-x: auto;  /* 顯示左右滾動條 */
            overflow-y: hidden; /* 禁止上下滾動條 */
            -webkit-overflow-scrolling: touch; /* 平滑滾動效果 */
            margin-top: 20px;
        }

    </style>
</head>
<body>

<div class="container">
    <div class="white-box">
        <h2 class="text-center mb-4">睡眠評估量表記錄</h2>

        <% let queryStringParam = table === "users" ? "user_id" : "man_user_id"; %>
        <% let recordId = userId || manUserId; %>


        <!-- 顯示使用者資訊 -->
        <p><strong>ID：</strong> <%= recordId || '未提供' %></p>
        <p><strong>名字：</strong> <%= user_name || '未提供' %></p>


        <div class="table-responsive">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>欄位名稱</th>
                        <% assessmentsData.forEach((_, index) => { %>
                            <th>紀錄 <%= index + 1 %></th>
                        <% }); %>
                    </tr>
                </thead>
<tbody>
    <tr>
        <td>ID</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_id %></td>
        <% }); %>
    </tr>
    <tr>
        <td>測試日期</td>
        <% assessmentsData.forEach(item => { %>
            <td>
                <%= new Date(item.sleep_test_date).toLocaleDateString('zh-TW', {
                    year: 'numeric', month: 'long', day: 'numeric'
                }) %>
            </td>
        <% }); %>
    </tr>
    <tr>
        <td>測驗量表</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_question_content %></td>
        <% }); %>
    </tr>
    <tr>
        <td>過去一個月來，您通常何時上床</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_answer_1_am_pm + ' ' + item.sleep_answer_1_a + '時' + item.sleep_answer_1_b + '分' %></td>
        <% }); %>
    </tr>
    <tr>
        <td>過去一個月來，您通常多久才能入睡</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_answer_2 + '分' %></td>
        <% }); %>
    </tr>
    <tr>
        <td>過去一個月來，您通常何時起床</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_answer_3_am_pm + ' ' + item.sleep_answer_3_a + '時' + item.sleep_answer_3_b + '分' %></td>
        <% }); %>
    </tr>
    <tr>
        <td>過去一個月來，您通常實際睡眠可以入睡幾小時</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_answer_4 + '時' %></td>
        <% }); %>
    </tr>

    <% const sleepQuestions = [
        "過去一個月內，您多常服用藥物幫助入睡",
        "過去一個月內，您多常在用餐、開車或社交場合活動時感到困倦，難以保持清醒",
        "過去一個月內，保持足夠的熱情去完成事情對您來說有多大問題",
        "過去一個月來，整體而言，您覺得自己的睡眠品質如何",
        "無法在30分鐘內入睡",
        "半夜或凌晨便清醒",
        "必須起來上廁所",
        "無法舒適呼吸",
        "大聲打呼或咳嗽",
        "會覺得冷",
        "覺得燥熱",
        "睡覺時常會做惡夢",
        "身上有疼痛"
    ]; %>

    <% sleepQuestions.forEach((question, index) => { %>
        <tr>
            <td><%= question %></td>
            <% assessmentsData.forEach(item => { %>
                <td><%= item[`sleep_answer_${index + 5}`] %></td>
            <% }); %>
        </tr>
    <% }); %>

    <tr>
        <td>睡眠困難分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_sleep_difficulty %></td>
        <% }); %>
    </tr>
    <tr>
        <td>睡眠時間分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_duration %></td>
        <% }); %>
    </tr>
    <tr>
        <td>睡眠效率分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_efficiency %></td>
        <% }); %>
    </tr>
    <tr>
        <td>睡眠干擾分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_disturbance %></td>
        <% }); %>
    </tr>
    <tr>
        <td>睡眠用藥分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_medication %></td>
        <% }); %>
    </tr>
    <tr>
        <td>白天功能分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_daytime_function %></td>
        <% }); %>
    </tr>
    <tr>
        <td>主觀睡眠品質分數</td>
        <% assessmentsData.forEach(item => { %>
            <td><%= item.sleep_score_subjective_quality %></td>
        <% }); %>
    </tr>
    <tr>
        <td>總分</td>
        <% assessmentsData.forEach(item => { %>
            <td class="<%= item.sleep_score_total >= 80 ? 'highlight' : '' %>">
                <%= item.sleep_score_total %>
            </td>
        <% }); %>
    </tr>
</tbody>



            </table>
        </div>

        <a href="/result?<%= queryStringParam %>=<%= recordId %>" class="btn btn-secondary mt-3">返回個人詳細資料</a>
    </div>
</div>

</body>
</html>
