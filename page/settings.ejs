<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登入頁面</title>
    <style>
        body {
            background: linear-gradient(to right, rgba(0, 123, 255, 0.1), rgba(0, 123, 255, 0.2)), 
                        url('https://via.placeholder.com/1500x1000/87CEFA/ffffff?text=Background+Image') no-repeat center center fixed;
            background-size: cover;
            font-family: 'DFKai-SB', '標楷體', DFKai-SB, serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            max-width: 400px;
            width: 100%;
            text-align: center;
        }
        h1 { margin-bottom: 20px; color: #007bff; }
        form { display: flex; flex-direction: column; }
        input {
            margin-bottom: 10px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }
        button {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover { background-color: #0056b3; }
        .options { margin-top: 15px; }
    </style>
</head>
<body>
    <div class="container">
        <% if (!userRole) { %>
            <h1>登入</h1>
            <form action="/settings/login" method="POST">
                <input type="text" name="employee_name" placeholder="帳號" required>
                <input type="password" name="employee_password" placeholder="密碼" required>
                <button type="submit">登入</button>
            </form>

            <% if (typeof loginError !== 'undefined' && loginError) { %>
                <p style="color: red;">❌ <%= loginError %></p>
            <% } %>

        <% } else if (typeof passwordResetRequired !== 'undefined' && passwordResetRequired) { %>
            <h1>需要重設密碼</h1>
            <form action="/reset_password" method="POST">
                <input type="hidden" name="settings_id" value="<%= settingsId %>">
                <input type="password" name="new_password" placeholder="新密碼" required>
                <button type="submit">更新密碼</button>
            </form>
        <% } else { %>
            <h1>歡迎，人員：<%= userRole %></h1>
            <div class="options">
                <% if (userRole === '管理者') { %>
                    <button onclick="window.location.href='/dashboard?settings_id=<%= settingsId %>&user_role=<%= userRole %>'">進入 Dashboard</button>
                <% } else if (userRole === '研究人員修改者' || userRole === '臨床人員修改者') { %>
                    <button onclick="window.location.href='/index'">進入資料查詢</button>
                <% } else { %>
                    <p>🔒 您沒有權限執行其他操作</p>
                <% } %>
            </div>
        <% } %>
    </div>
</body>
</html>
