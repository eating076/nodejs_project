<!DOCTYPE html>
<<<<<<< HEAD
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.6">
    <title>步數統計</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style> 
        body {
            background: linear-gradient(to right, #b9c2cd, #9dbce7); /* 漸變效果 */
            height: 100vh; 
            margin: 0; 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            font-family: Arial, sans-serif; 
            padding : 0; 
            } 
        </style> 
        <style> 
        body { 
            background: linear-gradient(to right, #b9c2cd, #9dbce7); /* 漸變效果 */ 
            height: 100vh; margin: 0; display: flex; justify-content: center; /* 水平居中 */ 
            align-items: flex-start; /* 改為頂部對齊 */ 
            font-family: Arial, sans-serif; padding: 10px; /* 增加整體的內邊距 */ 
            } 
            .left-bottom { 
                max-width: 400px; 
                margin: 40px; /* 減小上下間距 */ 
                padding: 65px;
                background-color: #e1d8e1; 
                border-radius: 8px; 
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); 
                flex: 1; 
            } 
                .line { width: 100%; /* 線條寬度 */ 
                height: 1px; /* 線條高度 */ 
                background-color: #ccc; /* 線條顏色 */ 
                margin: 20px 0; /* 與上下內容的間距 */ 
                } 
                .left-top { 
                width: 100%; 
                border-collapse: collapse; 
                text-align: center; 
                margin-right : 70px; 
                margin-left : 100px; 
                flex :1; 
                } 
                h1 { 
                    text-align: center; 
                    color: #333; 
                } 
                .profile-header { 
                    text-align: center; 
                    margin-bottom: 20px; 
                } 
                .profile-header img { 
                    width: 150px; 
                    height: 150px; 
                    border-radius: 50%;
                } 
                .profile-header h2 { 
                    margin: 10px 0; 
                    color: #333; 
                } 
                .profile-header p { 
                    color: #777;
                 } 
                .profile-info { 
                    margin-top: 20px; 
                } 
                .profile-info h3 { 
                    color: #333;
                } 
                .profile-info ul { 
                    list-style-type: none; 
                    padding: 0; 
                } 
                .profile-info ul li { 
                    margin: 10px 0; 
                    color: #555; 
                } footer { 
                    text-align: center; 
                    margin-top: 20px; 
                    color: #777; 
                } 
        </style>
</head>
<body>
    <div class="left-bottom">
        <div class="profile-header">
            <h2>個人基本資訊</h2>
             <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRrjDKvX46GOpgDflhYQ7KI9fewSC6y552-hA&s" alt="頭像" class="profile-picture">
        </div>
        <div class="line"></div>
        <div class="profile-info">
            <ul>
                <li><strong>姓名：</strong> 蔡昆佑</li>
                <li><strong>年齡：</strong> 25</li>
                <li><strong>生日：</strong> 2002/01/01</li>
                <li><strong>血型：</strong> O型</li>
                <li><strong>稱謂：</strong> 新手爸爸</li>
                <li><strong>身分證字號：</strong> F131420505</li>
                <li><strong>通訊地址：</strong> 新北市淡水區XX路XX段XX號XX樓</li>
                <li><strong>連絡電話：</strong> 0900-123-123</li>
            </ul>
        </div>

        <footer>
            <p>© 2025 xx醫院 | 版權所有</p>
        </footer>
    </div>
</body>
<style>
    body {
        font-family: "標楷體", Arial, sans-serif;
        background-color: #c2aecc;
        padding: 20px;
        margin: 0;
        display: flex;
        flex: 1;  /* 讓此區塊占據剩餘空間 */
    }

    .right-bottom {
        width: 90%;
        max-width: 900px;
        margin: auto;
        background: white;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        overflow-x: auto;
        position: relative;
        flex: 2;
        height: 700px; /* 固定高度 */
        overflow-y: auto; /* 允許垂直滾動 */
        margin-left : 50px;
    }

    .right-top {
        width: 100%;
        border-collapse: collapse;
        text-align: center;
        margin-right : 0px;
        margin-left : 50px;
        flex :1;
    }
    .settings-icon {
        position: absolute;
        top: 10px;
        right: 5px;
        font-size: 24px;
        cursor: pointer;
        color: #6b7280;
        transition: color 0.3s;
    }
    .settings-icon:hover {
        color: #4b5563;
    }
    th, td {
        border: 1px solid #ddd;
        padding: 8px;
    }
    th {
        background-color: #c8b1d7;
    }
    .score-low {
        color: red;
        font-weight: bold;
    }
    .weight-change {
        color: red;
    }
    .love-score {
        padding: 5px;
        border-radius: 5px;
        display: inline-block;
        width: 40px;
        text-align: center;
        color: rgb(3, 3, 3);
        font-weight: bold;
    }
    .btn-info {
        background-color: #007BFF;
        color: white;
        border: none;
        padding: 3px 6px;
        font-size: 12px;
        cursor: pointer;
        border-radius: 5px;
        margin-left: 5px;
    }
    .btn-info:hover {
        background-color: #0056b3;
    }

    /* 色階設定 */
    .level-0 { background-color: rgb(253, 255, 144); }
    .level-1 { background-color: #b1f3ba; }
    .level-2 { background-color: #7ee77e; }
    .level-3 { background-color: #07bd07; }
    .level-4 { background-color: #008000; }

    /* 彈出視窗 */
    .popup {
        display: none;
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: white;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        text-align: center;
        max-width: 300px;
        width: 80%;
    }
    .popup img {
        width: 100%;
    }
    .popup .close-btn {
        background-color: red;
        color: white;
        border: none;
        padding: 5px 10px;
        font-size: 14px;
        cursor: pointer;
        border-radius: 5px;
        margin-top: 10px;
    }

    .buttons {
        display: flex;
        justify-content: space-between;
        margin-top: 30px;
    }

    .btn {
        padding: 12px 20px;
        font-size: 18px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: 0.3s;
    }
    .btn-secondary {
        background-color: #6c757d;
        color: white;
    }

    .btn-secondary:hover {
        background-color: #565e64;
    }
    .buttons-container {
    display: flex;
    justify-content: space-around;
    margin-top: 20px;
}
.buttons-container button {
    flex: 1;
    margin: 0 5px;
    padding: 10px;
    border: none;
    border-radius: 5px;
    color: white;
    cursor: pointer;
}
.step-count { background-color: #28a745; }
.test-score { background-color: #ffc107; }
.analysis-result { background-color: #17a2b8; }
.progress-check { background-color: #6c757d; }
.goback{background-color: rgb(255, 0, 0);}
</style>
</head>
<body>
    <div class="right">
        <a href="/settings" class="settings-icon">
           <i class="fas fa-cog"></i>
        </a>
        <div class="right-top">
            <!-- 步數統計按鈕 -->
            <button class="step-count" onclick="window.location.href='/steps'">步數統計</button>
            <!-- 測驗分數按鈕 -->
            <button class="test-score">測驗分數
            <td>
                <select name="score-dropdown" onchange="handleSelection(this.value)">
                    <option value="" selected>請選擇量表</option>
                    <option value="support_assessment">生產支持知覺量表</option>
                    <option value="attachment_support">親子依附支持量表</option>
                    <option value="knowledge">母乳知識量表</option>
                    <option value="dour">產後憂鬱量表</option>
                    <option value="hurt">疼痛量表</option>
                    <option value="sleep">匹茲堡睡眠品質量表</option>
                </select>
            </td>
        </button>
            
            <script>
                function handleSelection(value) {
                    if (value) {
                        window.location.href = `/${value}`;
                    }
                }
            </script>
            
            
            <!-- 分析結果按鈕 -->
            <button class="analysis-result" onclick="window.location.href='/analysis'">分析結果</button>
            <!-- 進階查詢按鈕 -->
            <button class="progress-check" onclick="window.location.href='/advanced_search'">進階查詢</button>

            <button class="goback" onclick="window.location.href='/index'">重新查詢</button>
        </div>
        <hr style="margin-left: 100px;">
    <br>
    <style>
         body {
            font-family: "標楷體", Arial, sans-serif;
            background-color: #f3f4f6;
        }
        .container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
            width: 600px;
            height: 610px; /* 將高度設為 550px */
            overflow-y: auto; /* 確保內容過多時會出現滾動條 */
            text-align: left;
            position: absolute; /* 改為絕對定位 */
            bottom: 55px;       /* 距離底部 20px */
            right: 50px;        /* 距離右側 20px */
            top: auto;          /* 確保不受 top 影響 */
            left: auto;         
        }

        .title {
            font-size: 24px;
            margin-bottom: 10px;
        }
        .settings-icon {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 24px;
            cursor: pointer;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        table th, table td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: center;
        }
        table th {
            background-color: #c8b1d7;
        }
        .rate-button {
            padding: 5px 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .chart-container {
            display: none;
            margin-top: 30px;
        }
        .navigation-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }
        .navigation-buttons button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            background-color: #28a745;
            color: white;
        }
        .navigation-buttons button:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }
        .back-button {
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
            background-color: #6c757d;
            color: white;
            text-align: center;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }
        .btn {
            padding: 12px 20px;
            font-size: 18px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s;
        }


        .btn-secondary {
            background-color: #6c757d;
            color: white;
        }


        .btn-secondary:hover {
            background-color: #565e64;
        }


    </style>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        const stepsData = [
            { step_date: '2024/12/01', steps: 7500, goal: 10000 },
            { step_date: '2024/12/02', steps: 10570, goal: 10000 },
            { step_date: '2024/12/03', steps: 6500, goal: 10000 },
            { step_date: '2024/12/04', steps: 8200, goal: 10000 },
            { step_date: '2024/12/05', steps: 9400, goal: 10000 },
            { step_date: '2024/12/06', steps: 10200, goal: 10000 },
            { step_date: '2024/12/07', steps: 8700, goal: 10000 },
            { step_date: '2024/12/08', steps: 8900, goal: 10000 },
            { step_date: '2024/12/09', steps: 9600, goal: 10000 },
            { step_date: '2024/12/10', steps: 10100, goal: 10000 },
            { step_date: '2024/12/11', steps: 8500, goal: 10000 },
            { step_date: '2024/12/12', steps: 9200, goal: 10000 },
            { step_date: '2024/12/13', steps: 9300, goal: 10000 },
            { step_date: '2024/12/14', steps: 8800, goal: 10000 },
            { step_date: '2024/12/15', steps: 9700, goal: 10000 }
        ];
        let currentIndex = 0;


        function showChart(date) {
            document.querySelector('.chart-container').style.display = 'block';
            document.querySelector('table').style.display = 'none';


            const selectedIndex = stepsData.findIndex(item => item.step_date === date);
            currentIndex = Math.max(0, selectedIndex - 4);
           
            updateChart();
        }


        function updateChart() {
            const chartData = stepsData.slice(currentIndex, currentIndex + 5);
            const labels = chartData.map(item => item.step_date);  
            const steps = chartData.map(item => item.steps);


            const ctx = document.getElementById('stepsChart').getContext('2d');
            if (window.myChart) window.myChart.destroy();
            window.myChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: labels,
                    datasets: [{
                        label: '步數',
                        data: steps,
                        backgroundColor: 'rgba(54, 162, 235, 0.6)',
                        borderColor: 'rgba(54, 162, 235, 1)',
                        borderWidth: 1,
                        barThickness: 30
                    }]
                }
            });
            document.getElementById('prevBtn').disabled = currentIndex === 0;
            document.getElementById('nextBtn').disabled = currentIndex + 5 >= stepsData.length;
        }


        function prevPage() {
            currentIndex = Math.max(0, currentIndex - 5);
            updateChart();
        }


        function nextPage() {
            currentIndex = Math.min(stepsData.length - 5, currentIndex + 5);
            updateChart();
        }


        function backToSteps() {
            document.querySelector('.chart-container').style.display = 'none';
            document.querySelector('table').style.display = 'table';
        }

        window.onload = function() {
    const container = document.querySelector('.container');
    container.style.position = 'relative';
    container.style.transform = 'translateY(0)'; // 確保位置正常
};

        window.onload = function() {
            const tbody = document.querySelector("tbody");
            stepsData.forEach(item => {
                const rate = ((item.steps / item.goal) * 100).toFixed(1);
                tbody.innerHTML += `
                    <tr>
                        <td>${item.step_date}</td>  <!-- 改用 step_date -->
                        <td>${item.steps}</td>
                        <td>${item.goal}</td>       <!-- 改用 goal -->
                        <td>
                            <button class="rate-button" onclick="showChart('${item.step_date}')">${rate}%</button>
                        </td>
                    </tr>`;
            });
        }
    </script>
    </div>
</head>
<body>
    <div class="container">
        <div class="title">步數統計</div>
        <table>
            <thead>
                <tr>
                    <th>日期</th>
                    <th>今日步數</th>
                    <th>目標步數</th>
                    <th>完成率 (%)</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
        <div class="chart-container">
            <canvas id="stepsChart"></canvas>
            <div class="navigation-buttons">
                <button id="prevBtn" onclick="prevPage()">上一頁</button>
                <button id="nextBtn" onclick="nextPage()">下一頁</button>
            </div>
            <button class="back-button" onclick="backToSteps()">返回步數統計</button>
        </div>
    </div>


    <script>
        function goToSettings() {
            window.location.href = '/settings';
        }


        function goBack() {
            window.location.href = '/result';
        }
    </script>
</body>
</html>

=======
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>步數統計</title>
    <link rel="stylesheet" href="steps.css">
</head>
<body>
    <h1>步數統計</h1>

    <form id="stepForm">
        <label for="date">日期:</label>
        <input type="date" id="date" name="date" required>
        <br>
        <label for="steps">今日步數:</label>
        <input type="number" id="steps" name="steps" required>
        <br>
        <label for="goal">目標步數:</label>
        <input type="number" id="goal" name="goal" required>
        <br>
        <button type="button" id="calculateBtn">計算完成率</button>
    </form>

    <h2>完成率: <span id="completionRate">0%</span></h2>

    <nav>
        <ul>
            <li><a href="/about">返回查詢結果</a></li>
            <li><a href="/scores">測驗分數</a></li>
            <li><a href="/analysis">分析結果</a></li>
            <li><a href="/advanced-search">進階查詢</a></li>
        </ul>
    </nav>

    <script>
        document.getElementById('calculateBtn').addEventListener('click', function() {
            const steps = parseInt(document.getElementById('steps').value);
            const goal = parseInt(document.getElementById('goal').value);
            if (goal > 0) {
                const completionRate = ((steps / goal) * 100).toFixed(2);
                document.getElementById('completionRate').innerText = `${completionRate}%`;
            } else {
                alert('目標步數必須大於0');
            }
        });
    </script>
</body>
</html>
>>>>>>> 52f26e00f401ee33308441c48b0276c36ece463c
