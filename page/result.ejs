<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>個人檔案</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        body {
            background: linear-gradient(to right, #b9c2cd, #9dbce7); /* 背景漸層 */
            margin: 0;
            font-family: "標楷體", Arial, sans-serif; /* 設置字體 */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            display: flex;
            gap: 30px;
            width: 100%;
            max-width: 1200px;
        }

        /* 左側區塊樣式 */
        .left-section {
            flex: 1;
            background-color: #e1d8e1;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 30px;
            max-width: 400px;
        }

        .profile-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-header img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
        }

        .profile-header h2 {
            margin: 10px 0;
            color: #333;
        }

        .line {
            width: 100%;
            height: 1px;
            background-color: #ccc;
            margin: 20px 0;
        }

        .profile-info ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        .profile-info ul li {
            margin: 10px 0;
            color: #555;
            font-size: 16px;
        }

        footer {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #777;
        }

        /* 右側按鈕區塊樣式 */
        .right-section {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            gap: 15px;
        }

        .button {
            width: 100%;
            max-width: 300px;
            padding: 15px;
            font-size: 16px;
            font-weight: bold;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        /* 設定圖示 */
        .settings-icon {
            position: absolute;
            color: #6b7280;
            top: 20px;
            right: 20px;
            font-size: 24px;
            cursor: pointer;
        }

        .step-count {
            background-color: #28a745;
        }
        .step-count:hover {
            background-color: #218838;
        }

        .test-score {
            background-color: #ffc107;
        }
        .test-score:hover {
            background-color: #e0a800;
        }

        .analysis-result {
            background-color: #17a2b8;
        }
        .analysis-result:hover {
            background-color: #138496;
        }

        .progress-check {
            background-color: #6c757d;
        }
        .progress-check:hover {
            background-color: #5a6268;
        }

        .goback {
            background-color: #dc3545;
        }
        .goback:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- 設定圖示 -->
        <div class="settings-icon" onclick="goToSettings()">
            <i class="fas fa-cog"></i>
        </div>
        <!-- 左側區塊 -->
        <div class="left-section">
            <div class="profile-header">
                <h2>個人基本資訊</h2>
                <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRrjDKvX46GOpgDflhYQ7KI9fewSC6y552-hA&s" alt="頭像">
            </div>
            <div class="line"></div>
            <div class="profile-info">
                <ul>
                    <li><strong>姓名：</strong> 蔡昆佑</li>
                    <li><strong>年齡：</strong> 25</li>
                    <li><strong>生日：</strong> 2002/01/01</li>
                    <li><strong>血型：</strong> O型</li>
                    <li><strong>稱謂：</strong> 新手爸爸</li>
                    <li><strong>身分證字號：</strong> F131420505</li>
                    <li><strong>通訊地址：</strong> 新北市淡水區XX路XX段XX號XX樓</li>
                    <li><strong>連絡電話：</strong> 0900-123-123</li>
                </ul>
            </div>
            <footer>
                <p>© 2025 xx醫院 | 版權所有</p>
            </footer>
        </div>
        <!-- 按鈕區塊 -->
        <div class="right-section">
            <!-- 步數統計按鈕 -->
            <button class="button step-count" onclick="window.location.href='/steps'">步數統計</button>
            <!-- 測驗分數按鈕 -->
            <button class="button test-score">
                測驗分數
                <select name="score-dropdown" onchange="handleSelection(this.value)">
                    <option value="" selected>請選擇量表</option>
                    <option value="support_assessment">生產支持知覺量表</option>
                    <option value="attachment_support">親子依附支持量表</option>
                    <option value="knowledge">母乳知識量表</option>
                    <option value="dour">產後憂鬱量表</option>
                    <option value="hurt">疼痛量表</option>
                    <option value="sleep">匹茲堡睡眠品質量表</option>
                </select>
            </button>
            <!-- 分析結果按鈕 -->
            <button class="button analysis-result" onclick="window.location.href='/analysis'">分析結果</button>
            <!-- 進階查詢按鈕 -->
            <button class="button progress-check" onclick="window.location.href='/advanced_search'">進階查詢</button>
            <!-- 重新查詢按鈕 -->
            <button class="button goback" onclick="window.location.href='/index'">重新查詢</button>
        </div>
    </div>
    <script>
        function goToSettings() {
            window.location.href = '/settings';
        }

        function handleSelection(value) {
            if (value) {
                window.location.href = `/${value}`;
            }
        }
    </script>
</body>
</html>
