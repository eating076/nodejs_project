<!DOCTYPE html>
<<<<<<< HEAD
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>基本資料查詢</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        body {
            background: linear-gradient(to right, #b9c2cd, #9dbce7); /* 漸變效果 */
            height: 100vh; 
            margin: 0; 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            font-family: Arial, sans-serif; 
            padding : 0; 
            } 
        </style> 
    <style>
        body {
            font-family: "標楷體", Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f3f4f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            width: 90%;
            max-width: 600px;
            background: #fff;
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            position: relative;
            text-align: center;
        }
        .settings-icon {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 24px;
            cursor: pointer;
        }
        .settings-icon:hover {
            color: #007bff;
        }
        h1 {
            color: #333;
            font-weight: bold;
        }
        form {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            justify-content: space-between;
        }
        .form-group {
            width: 45%;
            display: flex;
            flex-direction: column;
            text-align: left;
        }
        .form-group label {
            margin-bottom: 5px;
            color: #333;
            font-weight: bold;
        }
        .form-group input,
        .form-group select {
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            outline: none;
        }
        .form-group input:focus,
        .form-group select:focus {
            border-color: #007bff;
        }
        .form-group .radio-group {
            display: flex;
            gap: 10px;
            align-items: center;
        }
        .btn-submit {
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            transition: background 0.3s;
            margin-top: 15px;
        }
        .btn-submit:hover {
            background-color: #0056b3;
        }
        .error-message {
            color: red;
            font-size: 14px;
            margin-top: 10px;
            display: none;
        }
    </style>
    <script>
        function validateForm(event) {
            event.preventDefault(); // 阻止表單自動提交
    
            const name = document.getElementById("user_name").value.trim();
            
            if (name === "") {
                alert("請輸入姓名！");
                return;
            }
    
            // 跳轉到模糊查詢結果頁面，將姓名帶入 URL
            window.location.href = `/fuzzy_search?name=${encodeURIComponent(name)}`;
        }
        function goToSettings() {
            window.location.href = '/settings';
        }
    </script>
    

</head>
<body>
    <div class="container">
        <!-- 設定圖示 -->
        <div class="settings-icon" onclick="goToSettings()">
            <i class="fas fa-cog"></i>
        </div>

        <h1>基本資料查詢</h1>

        <form id="userForm" onsubmit="validateForm(event)"> <!-- 添加了 id="userForm" -->
            <div class="form-group">
                <label for="user_name">姓名</label>
                <input type="text" id="user_name" name="user_name" placeholder="輸入姓名">
            </div>

            <div class="form-group">
                <label>性別</label>
                <div class="radio-group">
                    <label><input type="radio" name="user_gender" value="男"> 男</label>
                    <label><input type="radio" name="user_gender" value="女"> 女</label>
                </div>
            </div>

            <div class="form-group">
                <label for="user_salutation">稱謂</label>
                <select id="user_salutation" name="user_salutation">
                    <option value="新手媽媽">新手媽媽</option>
                    <option value="媽媽">媽媽</ption>
                    <option value="新手爸爸">新手爸爸</ption>
                    <option value="爸爸">爸爸</ption>
                </select>
            </div>

            <div class="form-group">
                <label for="user_birthdate">生日</label> <!-- user_birthdate 是正確的 id -->
                <input type="date" id="user_birthdate" name="user_birthdate"> <!-- 修改了 id -->
            </div>

            <div class="form-group">
                <label for="user_email">電子郵件</label>
                <input type="email" id="user_email" name="user_email" placeholder="輸入電子郵件">
            </div>

            <div class="error-message" id="errorMessage">⚠️ 查無此人</div>

            <button type="submit" class="btn-submit">查詢</button>
        </form>
    </div>

    <script>
        function validateForm(event) { <!-- 添加了 validateForm 函數 -->
            event.preventDefault(); // 阻止表單自動提交

            const name = document.getElementById("user_name").value.trim();

            if (name === "") {
                alert("請輸入姓名！");
                return;
            }

            // 跳轉到模糊查詢結果頁面，將姓名帶入 URL
            window.location.href = `/fuzzy_search?name=${encodeURIComponent(name)}`;
        }

        function goToSettings() {
            window.location.href = '/settings';
        }

        document.getElementById('userForm').addEventListener('submit', function(event) {
            event.preventDefault();
           
            const user_name = document.getElementById('user_name').value;
            const user_gender = document.querySelector('input[name="user_gender"]:checked').value;
            const user_salutation = document.getElementById('user_salutation').value;
            const user_birthdate = document.getElementById('user_birthdate').value; //<!-- user_birthdate 是正確的 id -->
            const user_email = document.getElementById('user_email').value;
=======
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>用戶註冊</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>基本資料查詢</h1>

    <form id="userForm">
        <label for="name">姓名:</label>
        <input type="text" id="name" name="name" required>
        <br>
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required>
        <br>
        <label for="age">年齡:</label>
        <input type="number" id="age" name="age" required>
        <br>
        <label for="address">住址:</label>
        <input type="text" id="address" name="address" required>
        <br>
        <label for="phone">電話號碼:</label>
        <input type="tel" id="phone" name="phone" required>
        <br>
        <label for="id_number">身分證字號:</label>
        <input type="text" id="id_number" name="id_number" required>
        <br>
        <label for="gender">性別:</label>
        <select id="gender" name="gender" required>
          <option value="male">男</option>
          <option value="female">女</option>
        </select>
        <br>
        <label for="birthdate">出生日期:</label>
        <input type="date" id="birthdate" name="birthdate" required>
        <br>
        <label for="height">身高 (cm):</label>
        <input type="number" step="0.01" id="height" name="height" min="0" required>
        <br>
        <label for="weight">體重 (kg):</label>
        <input type="number" step="0.01" id="weight" name="weight" min="0" required>
        <br>
        <label for="blood_type">血型:</label>
        <select id="blood_type" name="blood_type" required>
          <option value="A">A</option>
          <option value="B">B</option>
          <option value="AB">AB</option>
          <option value="O">O</option>
        </select>
        <br>
        <label for="emergency_contact_name">緊急聯絡人:</label>
        <input type="text" id="emergency_contact_name" name="emergency_contact_name" required>
        <br>
        <label for="emergency_contact_phone">緊急聯絡人電話:</label>
        <input type="tel" id="emergency_contact_phone" name="emergency_contact_phone" required>
        <br>
        <label for="betel_nut_habit">是否有咀嚼檳榔的習慣:</label>
        <select id="betel_nut_habit" name="betel_nut_habit" required>
          <option value="無">無</option>
          <option value="有">有</option>
        </select>
        <br>
        <label for="allergies">過敏源:</label>
        <input type="text" id="allergies" name="allergies" placeholder="花粉, 海鮮等" required>
        <br>
        <button type="submit">查詢</button>
    </form>

    <script>
        document.getElementById('userForm').addEventListener('submit', function(event) {
            event.preventDefault();
            
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            const age = document.getElementById('age').value;
            const address = document.getElementById('address').value;
            const phone = document.getElementById('phone').value;
            const id_number = document.getElementById('id_number').value;
            const gender = document.getElementById('gender').value;
            const birthdate = document.getElementById('birthdate').value;
            const height = document.getElementById('height').value;
            const weight = document.getElementById('weight').value;
            const blood_type = document.getElementById('blood_type').value;
            const emergency_contact_name = document.getElementById('emergency_contact_name').value;
            const emergency_contact_phone = document.getElementById('emergency_contact_phone').value;
            const betel_nut_habit = document.getElementById('betel_nut_habit').value;
            const allergies = document.getElementById('allergies').value;
>>>>>>> 52f26e00f401ee33308441c48b0276c36ece463c

            fetch('/users', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
<<<<<<< HEAD
                body: JSON.stringify({
                    user_name, user_gender, user_salutation, user_birthdate, user_email
                })
=======
                body: JSON.stringify({ name, email, age, address, phone, id_number, gender, birthdate, height, weight, blood_type, emergency_contact_name, emergency_contact_phone, betel_nut_habit, allergies })
>>>>>>> 52f26e00f401ee33308441c48b0276c36ece463c
            })
            .then(response => response.text())
            .then(data => {
                alert(data);
<<<<<<< HEAD
                window.location.href = '/result'; // 提交成功後跳轉到 fuzzy_search 頁面
=======
                window.location.href = '/about'; // 提交成功後跳轉到about頁面
>>>>>>> 52f26e00f401ee33308441c48b0276c36ece463c
            })
            .catch(error => {
                console.error('Error:', error);
            });
        });

<<<<<<< HEAD
        document.getElementById('user_birthdate').setAttribute('max', new Date().toISOString().split('T')[0]);
=======
        document.getElementById('birthdate').setAttribute('max', new Date().toISOString().split('T')[0]);
>>>>>>> 52f26e00f401ee33308441c48b0276c36ece463c
    </script>
</body>
</html>
